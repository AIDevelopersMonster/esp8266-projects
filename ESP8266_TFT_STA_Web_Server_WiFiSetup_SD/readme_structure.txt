

---

# 📁 Структура проекта

## 🔹 Файлы прошивки (ESP8266)

```
ESP8266_TFT_STA_Web_Server_WiFiSetup_SD.ino
wifi_provision.h
app_routes.h
sd_browser.h
img_draw.h
sd_test.h
```

---

### 📌 ESP8266_TFT_STA_Web_Server_WiFiSetup_SD.ino

Главный файл проекта.

Отвечает за:

* инициализацию SPI
* запуск TFT (ST7735)
* инициализацию SD карты
* запуск WiFi (AP/STA)
* запуск WebServer
* регистрацию маршрутов
* основной цикл обработки клиентов

---

### 📌 wifi_provision.h

Модуль настройки WiFi.

Реализует:

* режим точки доступа при первом запуске
* веб-форму ввода SSID и пароля
* сохранение конфигурации
* автоматический переход в STA режим
* кнопку сброса WiFi

---

### 📌 app_routes.h

Основные маршруты веб-приложения.

Содержит:

* страницы управления
* API команды
* логику основной части веб-интерфейса

---

### 📌 sd_browser.h

Модуль веб-файлового менеджера SD карты.

Реализует:

* `/files` — веб-интерфейс просмотра файлов
* `/api/list?dir=/...` — JSON список директории
* `/sd?path=/...` — отдача файлов браузеру
* `/api/show?file=/...` — вывод изображения на TFT

Обеспечивает безопасную работу с путями (защита от `..`).

---

### 📌 img_draw.h

Модуль вывода изображений на TFT.

Реализует:

* чтение BMP (16-bit и 24-bit)
* отрисовку по координатам
* центрирование 128×128 на дисплее 160×128
* поддержку canvas 160×128

---

### 📌 sd_test.h

Модуль диагностики SD карты.

Позволяет:

* проверить инициализацию
* вывести результат на TFT
* убедиться в корректности подключения

---

# 📂 Структура SD карты

```
/apriltag
/qr
/roadsigns
/roadsigns_test
/config
```

---

### 🔸 /roadsigns

Основная папка с изображениями дорожных знаков.
Поддерживаются BMP 128×128 и 160×128.

---

### 🔸 /roadsigns_test

Тестовая папка для отладки новых изображений.

---

### 🔸 /qr

Изображения QR-кодов.

---

### 🔸 /apriltag

Изображения AprilTag маркеров.

---

### 🔸 /config

Файлы конфигурации WiFi и системные данные.

---

# 🔄 Логика работы системы

1. ESP8266 подключается к WiFi.
2. Пользователь открывает веб-интерфейс.
3. Через `/files` выбирается изображение.
4. Нажатие кнопки **SHOW** отправляет запрос:

   ```
   /api/show?file=/roadsigns/example.bmp
   ```
5. Изображение читается с SD и выводится на TFT.

---

# 🧠 Архитектурная особенность

Проект построен модульно:

* Веб-логика отделена от логики вывода изображения.
* SD-браузер независим от основного приложения.
* Вывод изображений инкапсулирован в отдельном модуле.
* Легко масштабируется под ESP32.


