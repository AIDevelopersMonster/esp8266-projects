
---

# ESP8266 TFT STA Web Server + WiFi Setup (D6 Reset)

Проект на базе **ESP8266 (NodeMCU / ESP-12E)** с TFT дисплеем и встроенным Web Server.
Поддерживает автоматическое подключение к WiFi и аппаратный сброс настроек через кнопку **D6**.

---

## 🚀 Возможности

* 📶 Подключение к WiFi в режиме STA
* 🌐 Встроенный Web Server
* 🖥 Отображение статуса на TFT дисплее
* 🔁 Автоматический запуск режима настройки при отсутствии сети
* 🔴 Аппаратный сброс WiFi через кнопку D6 (удержание 3 секунды)
* 💾 Хранение SSID и пароля в EEPROM

---

## 🔧 Используемое оборудование

* ESP8266 (NodeMCU 1.0 / ESP-12E)
* TFT дисплей (ST7735 / ST7789 / TFT_eSPI)
* Кнопка сброса (подключена к D6 / GPIO12)
* Питание 5V / USB

---

## 📌 Подключение кнопки RESET

```
D6 (GPIO12) ---- Кнопка ---- GND
```

Используется `INPUT_PULLUP`, поэтому внешний резистор не нужен.

---

## 🔄 Как работает WiFi Reset

1. Удерживаем кнопку D6 ~3 секунды
2. EEPROM очищается
3. ESP8266 перезагружается
4. Устройство запускается в режиме настройки (AP Mode)

Пример кода сброса:

```cpp
WiFi.disconnect(true);
eepromClearAll();
ESP.restart();
```

---

## 📂 Структура проекта

```
ProjectFolder/
 ├── ProjectName.ino
 ├── wifi_provision.h
 ├── ...
```

⚠ Важно:
Файл `.ino` должен иметь то же имя, что и папка проекта.

---

## 🛠 Используемые библиотеки

* ESP8266WiFi
* ESP8266WebServer
* EEPROM
* TFT_eSPI (или Adafruit_ST77xx)
* SPI
* Wire

---

## ⚙ Настройки платы

Arduino IDE → Инструменты:

* Плата: **NodeMCU 1.0 (ESP-12E Module)**
* Flash Size: 4MB (FS: 1MB или по проекту)
* CPU Frequency: 80MHz
* Upload Speed: 115200

---

## 🧪 Типичные проблемы

### ❌ "undefined reference to setup"

Проверь, что компилируется `.ino`, а не `.h` файл.

### ❌ "function-definition is not allowed here"

Проверь корректность фигурных скобок `}`.

### ❌ Не подключается к WiFi

Используй аппаратный RESET через кнопку D6.

---

## 💡 Применение

* IoT устройства
* Панели управления
* Web интерфейсы для датчиков
* TFT мониторинг
* Устройства с возможностью перенастройки WiFi без перепрошивки

---

## 📺 Видео по проекту

(сюда можно вставить ссылку на YouTube)

---

## 📜 Лицензия

MIT / Open Source (по желанию)

---

